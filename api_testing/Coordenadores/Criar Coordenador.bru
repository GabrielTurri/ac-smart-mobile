meta {
  name: Criar Coordenador
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/coordinators
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json {
  {
    "name": "Novo",
    "surname": "Coordenador",
    "email": "novo.coordenador@exemplo.com",
    "password": "senha123"
  }
}
