meta {
  name: Delete Coordinator
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/coordinators/{{coordinatorId}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}
